// TODO: SSE proxy to backend /match — see PROJECT_PLAN.docx Section 11 + 4.3.
export const dynamic = "force-dynamic";

export async function POST() {
  return new Response("Not yet implemented", { status: 501 });
}
